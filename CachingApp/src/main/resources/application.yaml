spring:
  application:
    name: CachingApp

  datasource:
    url: jdbc:mysql://localhost:3306/caching-db?useSSL=false
    username: root
    password: Nithinreddy9491$


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  cache:
    type: redis
#  data:
#    redis:
#      host: localhost
#      port: 6379
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}


