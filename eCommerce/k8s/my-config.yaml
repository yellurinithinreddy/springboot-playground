apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
data:
  PGDATA: /var/lib/postgresql/data/pgdata
  INVENTORY_DB_SERVICE: inventory-db-service
  ORDER_DB_SERVICE: orders-db-service
  KAFKA_BROKER: kafka-service

#  # file format
#  name.properties: |
#    field=value1,value2
