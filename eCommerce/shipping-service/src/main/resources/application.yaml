spring:
  application:
    name: shipping-service





server:
  servlet:
    context-path: /shippings
  port:
    9050

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instances:
    prefer-ip-address: true
